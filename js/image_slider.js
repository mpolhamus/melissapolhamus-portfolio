
var slider = new IdealImageSlider.Slider({
    		selector: '#slider',
    		// height: 400, // Required but can be set by CSS
    		interval: 3500,
    		effect: 'fade',
		});
		slider.start();



// SIMON MALLS

var slider = new IdealImageSlider.Slider({
    		selector: '#slider-yp-2',
    		// height: 400, // Required but can be set by CSS
    		interval: 3500,
    		effect: 'fade',
		});
		slider.start();

var slider = new IdealImageSlider.Slider({
    		selector: '#slider-simon-2',
    		// height: 400, // Required but can be set by CSS
    		interval: 3500,
    		effect: 'fade',
		});
		slider.start();




// UP N OUT

var slider = new IdealImageSlider.Slider({
            selector: '#upnout-slider-1',
            // height: 400, // Required but can be set by CSS
            interval: 3500,
            effect: 'fade',
        });
        slider.start();


var slider = new IdealImageSlider.Slider({
            selector: '#upnout-slider-2',
            // height: 400, // Required but can be set by CSS
            interval: 3500,
            effect: 'fade',
        });
        slider.start();
